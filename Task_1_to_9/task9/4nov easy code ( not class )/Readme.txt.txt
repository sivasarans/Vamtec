# first open country file, then state, register and then main file
# if start with main file, it automatically create a register db 
# is start with register file, it automatically create a state and country db

bcz, open() method also creates if db not found, causes error
